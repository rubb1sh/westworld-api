cat << EOF > src/controllers/$1.ts
/*** -------------------------------------------- ***/
/*** This file has been auto-generated by the CLI ***/
/*** -------------------------------------------- ***/
import { Router } from 'express'

const router: Router = Router()

/**
 * Create $1
 */
router.post('/api/$1', function $1(request, response) {
    const $1 = {
    }

    return response.status(201).json($1)
})

/**
 * Edit $1
 */
router.patch('/api/$1', function $1(request, response) {
    return response.status(201).json({
        message: \`Post $1 works !\`,
    })
})

/**
 * Get one $1
 */
router.get('/api/$1/:id(\\d+)', function $1(request, response) {
    return response.status(200).json({
        message: \`Get $1 works !\`,
    })
})

/**
 * Get many $1
 */
router.get('/api/$1', function $1(request, response) {
    return response.status(200).json({
        message: \`Get $1 works !\`,
    })
})

/**
 * Delete one $1
 */
router.delete('/api/$1:id(\\d+)', function $1(request, response) {
    return response.status(200).json({
        message: \`Delete $1 works !\`,
    })
})

/**
 * Delete many $1
 */
router.delete('/api/$1', function $1(request, response) {
    return response.status(200).json({
        message: \`Delete $1 works !\`,
    })
})

export default router
EOF
