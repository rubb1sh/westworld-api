cat << EOF > src/controllers/$1.ts
/*** -------------------------------------------- ***/
/*** This file has been auto-generated by the CLI ***/
/*** -------------------------------------------- ***/
import { Router, Request, Response } from 'express';

const router: Router = Router();

// --- Bind a request handler to a URI >>
router.get('/api/$1', function requestHandler(request: Request, response: Response) {
    const status = 200;
    const body = { message: `$1 works !` };
    response.status(status).json(json);
    response.end();
})

// --- Export the router >>
export default router;
EOF