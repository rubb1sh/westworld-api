cat << EOF > src/routes/$1.ts
import { Router, Request, Response } from 'express'

const router: Router = Router()

/*** -------------------------------------------- ***/
/*** This file has been auto-generated by the CLI ***/
/*** -------------------------------------------- ***/

// --- Create your own handlers >>
function requestHandler(request: Request, response: Response) {
    response.status(200)
    const body = {}
    response.json(body)
    response.end()
}

// --- Bind each handler to a URI >>
router.get('/api/$1', requestHandler)

// --- Export the router >>
export default router
EOF